describe('triangleType', function() {
    it("returns an output of 'equilateral' if side_1 input is 2, side_2 input is 2, and side_3 input is 2", function() {
        expect(triangleType('equilateral')).to.equal(2, 2, 2);
    });
});
